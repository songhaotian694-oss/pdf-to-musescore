# PDF 乐谱转 MuseScore：零基础使用说明书

这篇说明面向第一次使用 Codex、MuseScore 和乐谱识别工具的用户。完成安装后，你只需要在 Codex 中告诉它乐谱文件的位置，Skill 会调用 Audiveris 识别，再用 MuseScore Studio 生成可编辑的 `.mscz` 文件、校对 PDF 和可选的 MIDI。

> 第一次使用建议先生成“可编辑校音草稿”。图片识谱无法保证每个音符完全正确，正式演奏前必须对照原谱逐页检查并试听。

## 1. 先认识会用到的东西

| 名称 | 用途 | 是否必须 |
|---|---|---|
| Codex | 读取 Skill、组织转换和检查结果 | 必须 |
| 本 Skill | 告诉 Codex 如何安全完成整套转换 | 必须 |
| MuseScore Studio 4 | 打开、编辑、播放和导出乐谱 | 必须 |
| Audiveris | 从 PDF 或清晰乐谱图片中识别音符 | 必须 |
| Python 3 | 运行结构、排版和播放验证脚本 | 必须 |
| Poppler `pdfinfo` | 检查 PDF 页数和有效性 | 必须 |
| MuseHub / MuseSounds | 提供更真实的音色 | 可选 |

Skill 不是 MuseScore 插件。它是 Codex 读取的一组本地文件，安装后通常位于：

```text
C:\Users\你的用户名\.codex\skills\pdf-to-musescore\
```

文件夹内必须直接看到 `SKILL.md`、`scripts`、`references` 等内容，不能多套一层同名文件夹。

## 2. 安装 MuseScore Studio 4

1. 打开 MuseScore 官方下载页：<https://musescore.org/en/download>
2. Windows 用户选择 **Download for Windows (64-bit)**，或选择 **MuseScore Studio 64-bit without MuseHub**。
3. 下载完成后运行安装程序，按默认选项安装即可。
4. 从开始菜单打开 **MuseScore Studio 4**，等待它正常进入主界面，然后关闭一次。

MuseHub 和 MuseSounds 不是完成转换所必需的。只想先转换和编辑乐谱，可以安装不带 MuseHub 的 MuseScore Studio；需要更真实的管弦乐音色时再安装 MuseSounds。

不要安装 MuseScore 3。本 Skill 面向 MuseScore Studio 4。仓库已在 4.7 系列上验证，安装官网当前稳定版后仍应执行第 7 节的依赖检查。

常见的程序位置如下，实际位置以你的电脑为准：

```text
C:\Program Files\MuseScore 4\bin\MuseScore4.exe
```

如果找不到：在开始菜单搜索 MuseScore，右键选择“打开文件所在的位置”，再查看快捷方式的目标路径。

## 3. 安装 Audiveris

1. 打开 Audiveris 官方 GitHub 发布页：<https://github.com/Audiveris/audiveris/releases/latest>
2. 展开该版本的 **Assets**。
3. Windows 64 位电脑下载名称类似 `Audiveris-版本-windows-x86_64.msi` 的安装包。
4. 运行安装包，按默认选项安装。
5. 安装后打开一次 Audiveris，确认能进入界面。

官方安装包自带 Java 运行环境，通常不需要另外安装 Java。常见程序位置是：

```text
C:\Program Files\Audiveris\Audiveris.exe
```

## 4. 安装 Python 和 Poppler

### 4.1 Python

从 Python 官方 Windows 下载页安装 64 位 Python 3：<https://www.python.org/downloads/windows/>

安装界面如果出现 **Add Python to PATH**，可以勾选。安装完成后打开 PowerShell，输入：

```powershell
python --version
```

如果命令不存在，再试：

```powershell
py --version
```

### 4.2 Poppler

Windows 没有由 Poppler 项目直接提供的常规安装程序。本 Skill 只需要 Poppler 中的 `pdfinfo.exe`。可使用常见的 Windows 预编译包：

<https://github.com/oschwartz10612/poppler-windows/releases/latest>

下载 ZIP 后解压到一个固定位置，例如：

```text
C:\Tools\poppler\
```

确认其中能找到类似路径：

```text
C:\Tools\poppler\Library\bin\pdfinfo.exe
```

解压后不要随意移动该文件夹，否则配置中的路径会失效。

## 5. 从 GitHub 安装本 Skill

仓库地址：<https://github.com/songhaotian694-oss/pdf-to-musescore>

### 方法 A：直接让 Codex 安装，推荐

在 Codex 新建一个任务，发送下面这句话：

```text
请使用 skill-installer，从下面的公开 GitHub 仓库根目录安装 Skill，名称设为 pdf-to-musescore：
https://github.com/songhaotian694-oss/pdf-to-musescore
```

Codex 会把仓库安装到个人 Skill 目录。如果目标文件夹已经存在，它不会直接覆盖；更新方法见第 13 节。安装结束后，新建一个 Codex 任务再使用它。

### 方法 B：手动下载 ZIP

1. 打开仓库页面。
2. 点击绿色 **Code** 按钮，再点 **Download ZIP**。
3. 解压 ZIP。
4. 将解压后的文件夹改名为 `pdf-to-musescore`。
5. 把它复制到：

```text
C:\Users\你的用户名\.codex\skills\
```

最终应当是：

```text
C:\Users\你的用户名\.codex\skills\pdf-to-musescore\SKILL.md
```

下面这种多套一层的结构是错误的：

```text
C:\Users\你的用户名\.codex\skills\pdf-to-musescore\pdf-to-musescore-main\SKILL.md
```

安装后重新打开 Codex，或至少新建一个任务。新任务的可用 Skills 列表中应出现 `pdf-to-musescore`。

## 6. 配置本机程序路径

进入 Skill 文件夹，将 `config.example.json` 复制一份并改名为：

```text
config.local.json
```

用记事本打开，根据电脑上的实际位置修改。例如：

```json
{
  "MuseScore": "C:/Program Files/MuseScore 4/bin/MuseScore4.exe",
  "Audiveris": "C:/Program Files/Audiveris/Audiveris.exe",
  "PdfInfo": "C:/Tools/poppler/Library/bin/pdfinfo.exe",
  "Tessdata": "C:/Program Files/Audiveris/app/tessdata",
  "Python": "C:/Users/你的用户名/AppData/Local/Programs/Python/Python313/python.exe"
}
```

注意：

- JSON 中推荐使用 `/`，避免 Windows 反斜杠转义问题。
- 示例中的 `Python313` 只是例子，必须换成实际版本和路径。
- `Tessdata` 不确定时可以先保留空字符串；无歌词的器乐谱通常不受影响。
- `config.local.json` 只保存在本机，不会提交到 GitHub。

在 Skill 文件夹空白处按住 Shift 并单击鼠标右键，选择“在终端中打开”，安装 Python 依赖：

```powershell
python -m pip install -r requirements.txt
```

如果你的电脑使用 `py` 命令，则运行：

```powershell
py -m pip install -r requirements.txt
```

## 7. 做一次安装自检

仍在 Skill 文件夹的 PowerShell 中运行：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\detect-dependencies.ps1
```

结果中应满足：

- `Audiveris.exists` 为 `true`；
- `MuseScore.exists` 为 `true`；
- `PdfInfo.exists` 为 `true`；
- `Python.exists` 为 `true`；
- `Python.structurePackagesReady` 为 `true`；
- `errors` 是空数组。

有任何一项为 `false`，先修正 `config.local.json` 中对应路径，再重新运行。不要在依赖检查失败时开始正式转换。

## 8. 准备原始乐谱

识别成功率主要取决于源文件：

- 优先使用原始导出的 PDF；其次使用清晰、端正、无阴影的扫描件。
- 图片建议按 `01.jpg`、`02.jpg`、`03.jpg` 的顺序命名。
- 截图不要带播放器光标、选中框、聊天浮窗或大面积水印。
- 每页应完整包含谱号、调号、拍号、小节线和页边缘。
- 同一文件夹中不要混放总谱、分谱和另一个乐章。
- 相邻截图如果重复拍到同一个系统，要在请求中说明；Codex 会先去重再识别。

本 Skill 的主输入是 PDF。如果你只有多张图片，可以让 Codex先按顺序合成 PDF，再开始转换。

## 9. 在 Codex 中开始第一次转换

新建一个 Codex 任务，使用完整路径发送请求。PDF 示例：

```text
请使用 $pdf-to-musescore，把
D:\乐谱\我的曲子\总谱.pdf
转换为 MuseScore 可编辑草稿。

输出放到：
D:\乐谱\我的曲子\转换结果

请导出 MIDI，自动检查全部页面；发现错误时对照源 PDF 尝试修正，无法唯一判断的地方保留为待人工校对，不要用小节编号偏移掩盖结构错误。
```

多张图片示例：

```text
请使用 $pdf-to-musescore，转换下面文件夹中按编号排列的全部乐谱图片：
D:\乐谱\我的曲子\原图

先检查是否有重复系统或缺页，再合成 PDF。输出可编辑 MSCZ、校对 PDF 和 MIDI到：
D:\乐谱\我的曲子\转换结果
```

如果 Codex 已经自动识别到这个 Skill，不写 `$pdf-to-musescore` 也可以；第一次使用时明确写出名称更稳妥。

转换中出现“需要选择页组”并不表示失败。Codex 会先检查每一页，判断哪些页属于总谱、分谱或不同乐章，避免把不连续的页面拼在一起。

## 10. 看懂转换结果

常见输出文件如下：

| 文件 | 用途 |
|---|---|
| `score.mscz` | MuseScore 可编辑工程，最重要的文件 |
| `score-proof.pdf` | 逐页检查排版和内容 |
| `score.mid` | 检查播放通道和在其他软件中试听 |
| `.mxl` / `.musicxml` | Audiveris 与 MuseScore 之间的交换文件 |
| `report.md` / `report.json` | 检查结论、警告和失败原因 |
| `correction-worklist.json` | 需要继续对照源谱修正的位置 |

状态含义：

- `editable_draft_ready_for_review`：已得到可编辑草稿，等待人工逐页校对。
- `editable_draft_needs_correction`：已经生成 MSCZ，但自动检查发现明确疑点。
- `completed_needs_manual_review`：严格检查通过，仍需人工检查音符和试听。
- `failed_*`：对应步骤失败，应查看报告，不能把现有文件当成成品。

## 11. 第一次打开 MuseScore 文件

1. 双击 `score.mscz`，或先打开 MuseScore Studio，再选择“文件 → 打开”。
2. 立即选择“文件 → 另存为”，保存一份人工修改副本，例如 `曲名-人工校正版.mscz`。
3. 按空格键开始或暂停播放。
4. 打开“视图 → 混音器”，确认声部名称和音色，例如 Violin、Trombone、Cello。
5. 同时打开源 PDF 和 `score-proof.pdf`，从第一页开始逐系统比较。

初学者优先检查：

1. 声部数量和名称是否正确；
2. 谱号、调号、拍号是否正确；
3. 小节编号是否连续；
4. 休止小节有没有多出或缺失；
5. 音高、节奏、附点、连音线和临时升降号；
6. 反复记号、速度、力度和歌词；
7. 播放时各声部进入位置和音色是否正确。

选中错误音符后，可以使用 MuseScore 顶部工具栏修改时值，用键盘上下方向键调整音高，用 Delete 删除选中的错误元素。修改前保留副本，遇到整段错位时先检查是否多了或少了一个实际小节。

需要导出时选择“文件 → 导出”，可生成 PDF、MusicXML、MIDI 或音频。普通“保存”得到的是 `.mscz` 工程文件。

## 12. 草稿模式和严格模式怎么选

第一次转换使用默认的 `draft` 草稿模式。即使部分内容有疑点，它也会尽量生成可以打开和继续编辑的 MSCZ，并把问题写入报告。

准备正式交付前，可以这样要求：

```text
请继续使用 $pdf-to-musescore 对这个人工校正版执行严格验收，使用 validated 模式；重新检查结构、编号补偿、排版、播放音色和 MIDI 程序号。
```

严格验收通过仍不代表自动测得了百分之百的音符准确率。最后一次逐页比对和完整试听不能省略。

## 13. 更新 Skill

安装器不会覆盖已经存在的 Skill。更新前：

1. 关闭正在运行的转换任务。
2. 把本机的 `config.local.json` 复制到别处备份。
3. 将旧的 `pdf-to-musescore` 文件夹改名为 `pdf-to-musescore-backup-日期`。
4. 按第 5 节从 GitHub 重新安装。
5. 把备份的 `config.local.json` 放回新文件夹。
6. 重新运行第 7 节的依赖检查。
7. 确认新版本可用后再删除旧备份。

也可以直接在 Codex 中发送：

```text
请先备份我现有 pdf-to-musescore Skill 的 config.local.json，再从
https://github.com/songhaotian694-oss/pdf-to-musescore
更新安装，并运行依赖检查。不要删除我的乐谱和历史转换结果。
```

## 14. 常见问题

### Codex 找不到 Skill

检查 `SKILL.md` 是否位于：

```text
%USERPROFILE%\.codex\skills\pdf-to-musescore\SKILL.md
```

如果路径正确，新建一个 Codex 任务；仍然没有时重启 Codex。

### MuseScore 能打开，但播放是钢琴

查看报告中的 `playbackValidation`。只有声部名称正确还不够；必须让最终 MSCZ 和新导出的 MIDI 都使用目标乐器程序号。把问题交给 Skill 重新设置和验证，不要只在界面中改名称。

### 小节编号从中间开始错位

不要手工添加编号偏移。应找到第一个漂移点，检查那里是否多出空小节、重复休止或缺少实际小节。Skill 会拒绝使用 `noOffset` 等显示补偿冒充修复。

### 排版页数很多或最后一页很空

页数变化本身不一定是错误。默认 `reflow` 会清除导入产生的硬换行和硬分页，再让 MuseScore 自动排版。最后一页只有少量系统时可以在人工校正版中调整页面和系统间距，但不要为减少页数删掉小节。

### 图片识别错误很多

重新获得更清晰的源文件通常比反复调整 OMR 参数有效。优先处理倾斜、低分辨率、阴影、裁边、播放光标、重复截图和缺页。复杂手写谱、严重模糊谱和密集多声部总谱可能只能得到校音草稿。

### PowerShell 提示禁止运行脚本

只对当前命令使用：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\detect-dependencies.ps1
```

不需要永久修改整个系统的执行策略。

## 15. 最短操作清单

1. 安装 MuseScore Studio 4、Audiveris、Python 和 Poppler。
2. 从 GitHub 安装 `pdf-to-musescore` 到个人 Codex Skills 目录。
3. 创建并填写 `config.local.json`。
4. 安装 `requirements.txt` 中的 Python 依赖。
5. 运行 `detect-dependencies.ps1`，确保没有错误。
6. 新建 Codex 任务，给出源乐谱和输出文件夹的绝对路径。
7. 打开生成的 `score.mscz`，另存人工校正版。
8. 对照源 PDF 逐页检查，并完整试听一次。

